{
  "name": "@sessionbus/terminal-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "bun test test/*.test.ts"
  },
  "dependencies": {
    "@sessionbus/adapter-sdk": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
